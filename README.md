# qiven-host

Bootstrap repository for the `qiven::host` native C++ application.

This README is bootstrap-only and belongs to this repository after generation. Replace it with product documentation as the application is designed. Shared engineering and native build-system conventions are under `docs/engineering/`.
